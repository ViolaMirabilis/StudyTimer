<Page x:Class="StudyTimer.View.StudyTimerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudyTimer.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="750"
      Title="StudyTimerView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Width="440" Height="80" Background="White" CornerRadius="25" Opacity="0.25">
            <TextBox Height="75" Width="400" Background="Transparent" BorderThickness="0" Foreground="White" Opacity="0.50"
                     HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48" FontFamily="Bahnschrift"
                     TextAlignment="Center" VerticalContentAlignment="Center"/>
        </Border>
        
        <Border x:Name="TimerBackground" Grid.Row="1" Grid.Column="1"  Height="100" Width="250" Background="White" CornerRadius="25" Opacity="0.25"/>
        <TextBlock x:Name="tbTimer" Text="{Binding CurrentTime}"  Grid.Row="1" Grid.Column="1" FontFamily="Bahnschrift SemiLight" FontWeight="Bold" FontSize="64"
           HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <WrapPanel x:Name="RowOfButtons" Grid.Row="2" Height="50" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button x:Name="Play" Content="Play" Height="35" Width="45" Margin="15, 0, 15, 0"/>
            <Button x:Name="PauseResumeToggle" Content="Pause" Height="35" Width="45"/>
            <Button x:Name="Stop" Content="Stop" Height="35" Width="45" Margin="15,0,15,0"/>
        </WrapPanel>

    </Grid>
</Page>
